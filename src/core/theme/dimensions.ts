/** Shared sizing constants, incl. accessible minimum touch targets. */
export const dimensions = {
  /** Apple HIG / Material minimum recommended touch target. */
  minTouchTarget: 44,
  iconSm: 16,
  iconMd: 24,
  iconLg: 32,
  avatarSm: 32,
  avatarMd: 48,
  avatarLg: 72,
  buttonHeight: 50,
  inputHeight: 48,
  topBarHeight: 56,
  maxContentWidth: 640,
} as const;
